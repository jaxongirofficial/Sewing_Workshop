/// Application-wide constants. Styling values live under `config/theme`.
abstract final class AppConstants {
  static const String appName = 'Sewing Workshop';

  /// Splash minimum display duration for branding and layout settle.
  static const Duration splashMinDuration = Duration(milliseconds: 1600);
}
